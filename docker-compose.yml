version: '3'
services:
  recipe-back:
    container_name: recipe-back
    networks:
      - recipe-network
    build:
      context: ./
    ports:
      - '8081:8081'

networks:
    recipe-network:
      external:
        name: recipe-network